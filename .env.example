# ===== DJANGO =====
DEBUG=True
SECRET_KEY=ta_clé_django  # Générer avec : python manage.py shell -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
ALLOWED_HOSTS=localhost,127.0.0.1,ton_domaine.com

# ===== BASE DE DONNÉES (MySQL) =====
DB_ENGINE=django.db.backends.mysql
DB_NAME=nom_de_ta_base
DB_USER=ton_utilisateur_mysql
DB_PASSWORD=ton_mot_de_passe_mysql  # ⚠️ À remplacer par TA valeur locale
DB_HOST=localhost
DB_PORT=3306

# ===== CORS (Frontend Symfony) =====
CORS_ALLOWED_ORIGINS=http://localhost:8001,http://127.0.0.1:8001,http://ton_frontend.com

# ===== DRF & JWT =====
JWT_SECRET_KEY=génère_une_clé_avec_openssl_rand_hex_32  # Ex: openssl rand -hex 32
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_LIFETIME=30
JWT_REFRESH_TOKEN_LIFETIME=1440
